import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pokePedia',
  appName: 'Pokepedia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
